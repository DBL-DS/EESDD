<UserControl x:Class="EESDD.Widgets.Selector.SelectorButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             x:Name="OpBtn">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/StyleDictionary.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border x:Name="border" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="{x:Null}" CornerRadius="5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" Direction="-90" RenderingBias="Quality" ShadowDepth="0" Opacity="0.75"/>
        </Border.Effect>
        <Button Style="{StaticResource NoDefaultHover}" x:Name="btn" Click="Button_Click" Background="#ff777777">
            <Image Source="{Binding ImageSource, ElementName=OpBtn}" x:Name="image" Stretch="Fill"></Image>
        </Button>
    </Border>
</UserControl>
